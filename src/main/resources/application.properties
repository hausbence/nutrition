recipe.url=https://api.spoonacular.com/recipes/
ingredient.url=https://api.spoonacular.com/food/ingredients/
plannerconnect.url=https://api.spoonacular.com/users/connect
plannerbase.url=https://api.spoonacular.com/mealplanner/

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:postgresql://localhost:5432/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

# App Properties
bezkoder.app.jwtSecret= bezKoderSecretKey
bezkoder.app.jwtExpirationMs= 864000000

#spring.mvc.converters.preferred-json-mapper=gson
